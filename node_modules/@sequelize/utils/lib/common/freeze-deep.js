"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.freezeDescendants = exports.freezeDeep = void 0;
const is_plain_object_js_1 = require("./predicates/is-plain-object.js");
function freezeDeep(obj) {
    Object.freeze(obj);
    freezeDescendants(obj);
    return obj;
}
exports.freezeDeep = freezeDeep;
/**
 * Only freezes the descendants of an object, not the object itself.
 *
 * @param obj
 */
function freezeDescendants(obj) {
    for (const descendant of Object.values(obj)) {
        if ((0, is_plain_object_js_1.isPlainObject)(descendant) || Array.isArray(descendant)) {
            freezeDeep(descendant);
        }
    }
    return obj;
}
exports.freezeDescendants = freezeDescendants;
